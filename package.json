{
  "name": "pi-orchestrate",
  "version": "0.0.0",
  "private": true,
  "description": "Pi /orchestrate \u2014 Feature \u2192 Tasks, one Feature PR, git-workflow latch.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "orchestrate",
    "git-workflow"
  ],
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "pi": {
    "extensions": [
      "./src/orchestrate.ts",
      "./src/pr-await-latch.ts",
      "./src/git-workflow-guard.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.4",
    "@earendil-works/pi-tui": "^0.84.4",
    "@types/node": "^26.4.0",
    "fast-check": "^4.9.0",
    "typescript": "^7.0.2"
  }
}
